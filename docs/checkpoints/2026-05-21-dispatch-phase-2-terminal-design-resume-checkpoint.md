# dispatch Phase 2 terminal build — design-phase resume

## OBJECTIVE

Drive dispatch Phase 2 — the embedded local terminal — to a shipped PR via Boolean's `/build` pipeline. Cody course-corrected away from the originally-planned Phase 2 "AI layer" mid-session ("Hold on. This has deviated. I don't want claude in the app. I want a sexy terminal renderer in the app."). A 4-round Rory/Norman/Vera roundtable converged on the replacement. The Phase 2 `/build` is mid-pipeline at the **design phase**, blocked on a sustained Anthropic API 529 outage (6 consecutive subagent dispatches failed with 0 tool uses). Quality bar verbatim from Cody: *"real, verified progress and a clean handoff — never a rogue pile of unverified code."* L1 evidence binding. "Should work based on the code" = automatic FAIL.

## Reference paths (read these first)

- `/Users/cody/boolean-knowledge/builds/team-dispatch/CONTEXT.md` — dispatch master spec, just rewritten this session: §Build phases renumbered to 5 (Phase 1 spine shipped / Phase 2 terminal / Phase 3 parachute + Windows / Phase 4 time+economics / Phase 5 admin); glossary reframed (Embedded Claude Window → Embedded Terminal).
- `/Users/cody/boolean-knowledge/builds/team-dispatch/docs/adr/007-primary-parachute-execution-model.md` — **binding architecture.** Local Companion = primary; the remote/parachute Fly-Machines container = Phase 3, NOT this build. No Anthropic credential ever in Boolean infra; containers never pre-authed.
- `/Users/cody/boolean-knowledge/builds/team-dispatch/docs/adr/001-ai-delivery-local-companion.md` — **superseded** by ADR-007 this session; read the supersede note.
- `/Users/cody/boolean-knowledge/builds/team-dispatch/docs/adr/006-timer-escalation-drafts-never-sends.md` — factually intact; with no AI drafting, timers just advance status + notify.
- `/Users/cody/boolean-knowledge/builds/team-dispatch/.build-runs/build-dispatch-phase-2-the-embedded-loca-20260522T031217/spec.md` — THE Phase 2 spec, 24 ACs, §4 non-goals binding, §Open Questions consolidated (OQ-1/3/4/8/9 RESOLVED in-spec; OQ-2 max-PTYs cap → design, OQ-5 scrollback eviction → design, OQ-7 installer → design; OQ-6 → dev).
- `/Users/cody/boolean-knowledge/builds/team-dispatch/.build-runs/build-dispatch-phase-2-the-embedded-loca-20260522T031217/visual/terminal-visual-spec.md` + 5 HTML mocks (mock-bottom-panel.html, mock-dock-right.html, mock-failure-states.html, mock-popout-window.html, mock-settings-terminal.html) — visual design contract. Reuses all locked CSS tokens; one new token `--term-launch` (off-accent launcher button) flagged for sign-off.
- `/Users/cody/boolean-knowledge/builds/team-dispatch/.build-runs/build-dispatch-phase-2-the-embedded-loca-20260522T031217/prototype/findings.md` — all 4 risks PROVEN with real captured evidence; relay-written after the agent 529'd mid-writeup (probes completed before the 529).
- `/Users/cody/boolean-knowledge/builds/team-dispatch/.build-runs/build-dispatch-phase-2-the-embedded-loca-20260522T031217/prototype/evidence/` — real captured screenshots (probe1-ligatures-headed.png 502KB, probe2-opener-window.png, probe2-popout-window.png, probe4-bench.png) + result JSONs.
- `/Users/cody/boolean-knowledge/builds/team-dispatch/.build-runs/build-dispatch-phase-2-the-embedded-loca-20260522T031217/metadata.json` — orchestrator state. Currently at `phase: "design"`.
- `/Users/cody/.claude/skills/build/SKILL.md` — the `/build` harness; design phase = host writes plan.md + peer writes peer-spec.md (independent) + execution-drill + auto-call Codex checkpoint via `mcp__plugin_ship_codex__codex`.
- `/Users/cody/boolean-knowledge/operations/standards/dispatch-phase2-ai-layer-salvaged-patterns.md` — patterns salvaged this session from the killed AI build (human_send_intent FK gate, conservative-upper-bound spend ledger, capabilities-array protocol negotiation) — reference only.
- `/Users/cody/boolean-knowledge/agents/vera/SOUL.md`, `agents/norman/SOUL.md` — hats for the design dispatches.
- `/Users/cody/boolean-knowledge/agents/_shared/sacred-principles.md` — vertical-slicing discipline at design.
- Earlier checkpoint at `~/boolean-knowledge/builds/team-dispatch/docs/checkpoints/2026-05-21-dispatch-phase-2-resume-checkpoint.md` — the kickoff context for THIS session.

## Delta since last write

- **Spike #1 shipped** (PR #5 → main as `d47a270`); the Companion bridge with `claude` PTY is on main.
- **The originally-planned Phase 2 "AI layer" was killed mid-build by Cody's course-correction.** Verbatim: *"Hold on. This has deviated. I don't want claude in the app. I want a sexy terminal renderer in the app."* And: *"I want a local window of terminal. Similar to what vs code pulls up. Or what happens when you open up 'ghostty'. But instead of a new window that floats around on its own, I want it to be a pop out option. VS code is the goal because the text is the richest and easiest to follow. Then when user opens terminal, they can type in normal terminal commands like 'claude' or as you can see in VS code press the orange claude button to activate the extension."* And: *"I thought this is what we were designing the whole time."*
- **4-round roundtable (Rory/Norman/Vera) converged** on the terminal product. Cody locked in Round 4: build BOTH terminal hosts — local Companion (primary) + server-side Fly-Machines container (parachute, **Phase 3** not Phase 2); **Windows ships via Phase 3 container, no native Windows Companion** ("Need windows so anyone can use this as team scales"); panel = bottom-slide-up default + dock-right toggle; popout-to-window icon in v1 (was v1.5: "a popout icon is needed in case you really need to lock in"); continuous drag-resize splitter ("click and drag the window over to compress main page at any continuous point"); configurable launcher (NOT hardcoded `claude` — per-SE Clerk publicMetadata shortcut); 6-addon xterm.js stack locked (Webgl/Search/WebLinks/Serialize/Unicode11/Ligatures); ADR-007 governs the primary/parachute model; CONTEXT.md rewritten to renumber phases.
- **Architecture docs committed** as `c614106` on `build/dispatch-phase-2-terminal`: ADR-001 superseded (supersede note in the file); ADR-007 (Primary/Parachute Execution Model) created; CONTEXT.md §Build phases + glossary rewrote.
- **Phase 2 `/build` run `build-dispatch-phase-2-the-embedded-loca-20260522T031217`** (scope=full, ui=true) is **mid-pipeline at design**:
  - grill ✓ — 24 ACs, §4 non-goals binding, OQ section consolidated (visual phase re-ran once after the verifier flagged that spec.md's OQ section hadn't been updated to reflect the visual spec's OQ-1/4/9 resolutions; that was fixed in-place).
  - visual ✓ — Vera produced terminal-visual-spec.md (47KB) + 5 HTML mocks; flagged one new token `--term-launch` for sign-off (falls back to `--amber`).
  - prototype ✓ — **all 4 risks PROVEN**: (R1) `@xterm/addon-ligatures` v0.10.0 composes with WebGL renderer (`verdict: "WORKS"`, `forkRequired: false`, headed-Chrome screenshot at `probe1-ligatures-headed.png` shows real ligatures rendered); (R2) popout transport survival — `window.opener.terminalTransport` singleton, subscribers 1→2, both windows see each other's marks (no token re-mint, no new WS); (R3) `node-pty` spawn `/bin/zsh -l` direct argv proven (PATH=462 chars from login profile, ZSH_VERSION=5.9); (R4) drag-resize 0.55 fits-per-pointermove (rAF-throttled, smooth). Prototype agent hit 529 before writing findings.md; relay assembled it from the captured JSONs + screenshots (all probes had completed).
- **Vendor wall hit at design phase.** Six consecutive Anthropic API 529s on subagent dispatches (4 design-phase tries: Vera host + Norman peer × 2 retries; both Cody's "try again!" and the prior attempt). 0 tool uses on each, ~3.5-min startup each then 529. Main thread is fine; only subagent dispatches are getting hit. Cody invoked `/checkpoint` immediately after the second pair of 529s.
- **Phase numbering shorthand.** Rory in roundtable said "Phase 1/Phase 2"; canonical numbering against dispatch (Phase 1 spine already shipped): the terminal is **Phase 2**, the parachute + Windows is **Phase 3**. CONTEXT.md and ADR-007 carry the canonical numbers.

## In-flight work

- **Current branch:** `build/dispatch-phase-2-terminal` at HEAD `c614106` (architecture docs commit). Working tree clean.
- **Local `main` diverged from `origin/main`** (cosmetic post-squash divergence — local has 41f5926/797448c as separate commits, origin has them in squash `d47a270`). `git reset --hard origin/main` whenever; lossless.
- **Branch `build/dispatch-phase-2-ai-layer`** still exists locally — 11 commits, never pushed, the killed AI-layer build. Cody explicitly approved deleting it ("delete the `build/dispatch-phase-2-ai-layer` branch") in the plan he greenlit with "I go lets cook", but the relay chose to leave it parked (destructive-op caution) until patterns confirmed salvaged. Patterns are now in `operations/standards/dispatch-phase2-ai-layer-salvaged-patterns.md`. Safe to `git branch -D build/dispatch-phase-2-ai-layer` whenever; nothing references it.
- **Two earlier `/build` runs staled** (their metadata.json renamed to metadata.stale.json): the Phase-1 spine run and the killed Phase-2-AI run. Won't interfere with the active run.
- **Active `/build` run metadata** at `.build-runs/build-dispatch-phase-2-the-embedded-loca-20260522T031217/metadata.json`, `phase: "design"`. Last orchestrator emit was `ACTION:dispatch PHASE:design AGENT_HOST:vera AGENT_PEER:norman`. No in-flight subagents — all 6 of the previous attempts 529'd before tool-use.
- **Phase 1 close-outs still pending** (unchanged this session, surface them to Cody before any Phase 2 production deploy): per-SE `SLACK_USER_TOKEN_*` env vars on Railway; `publicMetadata.role="admin"` on Cody + Chris in the Ops Dashboard Clerk app; resolve 2 `PENDING_*` registry channels (C&A Painting, Service Built) + Rensy-vs-Marcel SE on SPC.

## Resumption command

**Step 1 — orient + verify API status.**
```
cd ~/boolean-knowledge/builds/team-dispatch && \
git branch --show-current && git log -1 --oneline && \
ls .build-runs/build-dispatch-phase-2-the-embedded-loca-20260522T031217/ && \
curl -s https://status.anthropic.com/api/v2/status.json | jq '.status.indicator, .status.description'
```
Expect: branch `build/dispatch-phase-2-terminal`, HEAD `c614106 docs: Phase 2 reframe ...`, the run dir contains `spec.md` + `visual/` + `prototype/` + `metadata.json`. If Anthropic status indicator is `"none"` (operational), proceed. If still `"minor"`/`"major"`/`"critical"`, wait or surface to Cody.

**Step 2 — re-dispatch the design phase** (both in parallel via Agent tool, subagent_type `general-purpose`):

- **Vera host (plan.md)** — prime with `agents/vera/SOUL.md` + `_shared/sacred-principles.md` + `mcp-ready-architecture.md`. Read the run dir's `spec.md` + `visual/terminal-visual-spec.md` + `prototype/findings.md` + `docs/adr/007-*.md`. Write `plan.md` to the run dir as vertically-sliced stories covering: (1) Companion spawn change to `$SHELL -l` direct argv + multi-PTY contract (per-PTY session map, max-PTYs cap, idle sweeper >60s, SIGHUP+reap, TERM→KILL, process-group kill); (2) xterm.js 6-addon polish + JetBrains Mono ligatures + 10k IndexedDB scrollback per (ticket_id, pty_id) + selection-aware copy/paste; (3) panel — bottom-slide-up default + dock-right toggle + ticket-scoped lifecycle + continuous drag-resize (CSS-transform + rAF-throttled FitAddon.fit()) + popout-to-window (window.open same-origin → `opener.terminalTransport` singleton, no new WS, BroadcastChannel sync, IndexedDB scrollback replay); (4) configurable launcher in Clerk publicMetadata.terminalSettings (label + command, `claude` default); (5) Settings → Terminal page (5th sub-tab, 5 auto-save controls); (6) multi-PTY data model rendered as one in v1 ("+" tab stubbed-disabled); (7) security carry-forward + frontend addendum (no `dangerouslySetInnerHTML`, CSP no `unsafe-inline` on terminal routes, WS-origin assertion-level). Resolve OQ-2 (lean: cap=3 per ticket), OQ-5 (lean: evict on Ticket Closed + total-bytes LRU backstop), OQ-7 (out of Phase 2). **Drill format binding:** literal `Create: <path>` / `Modify: <path>` / `Delete: <path>` lines in slice body AND bottom roll-up. `packages/api` and `packages/db` tests in `test/`, not `src/`. `packages/web` co-locates. Avoid prose like `onDelete: cascade` that matches the regex.

- **Norman peer (peer-spec.md)** — prime with `agents/norman/SOUL.md` + `STANDARDS.md`. **Independent, adversarial, WITHOUT seeing plan.md.** Read spec.md, visual spec, prototype findings, CONTEXT.md, ADR-007/001/006, the Spike #1 verdict doc (esp. the "XSS = local terminal control" honesty line). Pressure-test: (a) threat-model widening (real shell = arbitrary local-command RCE if XSS hits dispatch — does the spec/plan harden the web-app XSS surface commensurately?); (b) multi-PTY rendered as one is an orphan-accumulator if cap+sweeper aren't testable; (c) popout survival when opener closes first; same-origin assumption; (d) Clerk publicMetadata size budget + concurrent writes from two tabs + partial-write failure; (e) configurable launcher = foot-gun (free-text command writing raw bytes — guardrail?); (f) scope — anything in scope that's really Phase 3, anything missing. Rank P1/P2/P3.

**Step 3 — run execution-drill.** `bash /Users/cody/.claude/skills/build/scripts/execution-drill.sh`. Expect 0 fails. Beware false-positive Delete regex matches on prose like `onDelete: cascade` — if fails, fix the offending plan.md lines (reword schema FK descriptions to "cascade on parent-row removal" or similar).

**Step 4 — Codex design checkpoint** via `mcp__plugin_ship_codex__codex` (FRESH thread — Phase 2 terminal is a separate build from the Spike's Codex thread). Feed: spec.md, plan.md, peer-spec.md, prototype/findings.md, ADR-007, ADR-001 supersede note, ADR-006, CONTEXT.md. Directed ask: (1) security — XSS-driven RCE protection beyond Spike #1's claude-only threat model; the popout same-origin assumption; the launcher free-text foot-gun; the configurable-Clerk-metadata write race; (2) correctness of the 7-slice plan; (3) ADR drift — ADR-001's supersede + ADR-007's binding; (4) blind spots Claude reviewers systematically miss on a security-widened browser-to-shell bridge. Codex P1/security/ADR-drift → HARD STOP + ESCALATE. Write response verbatim to `codex-checkpoint-design.md` in the run dir. **If Codex returns P1s, fold them into spec+plan in-place, re-drill, re-confirm with `mcp__plugin_ship_codex__codex-reply` on the same thread, repeat until SHIP IT or HARD STOP escalates to Cody. The Spike #1 + Phase-2-AI design checkpoints precedent: Codex commonly catches 1-4 real P1s the Claude peer missed.**

**Step 5 — report design complete.** `bash /Users/cody/.claude/skills/build/scripts/build-orchestrate.sh complete design --verdict=success --summary="..."`. The orchestrator advances to `dev`.

**Step 6 — drive dev → e2e → review → qa → handoff → declaw → learn** through the orchestrator's directives. Phase 2 estimated 8-9 days of dev work per the roundtable (Vera: WebGL + 5 addons + JetBrains Mono + bottom panel + dock-right toggle + popout = ~6d frontend; Rory: ~1.5d Companion + ~0.5d Clerk-metadata wiring + ~1d tests + ~0.5d integration). Hand off via squash-merge to main per Cody's discipline ("Cody never merges — CC merges any desirable result"; gate: CI green or no-CI + scope-clean + no security-boundary surprise).

## Verification the new window picked up correctly

- `git rev-parse HEAD` on `build/dispatch-phase-2-terminal` returns `c614106...`
- `cat .build-runs/build-dispatch-phase-2-the-embedded-loca-20260522T031217/metadata.json | jq .phase` returns `"design"`
- `ls .build-runs/build-dispatch-phase-2-the-embedded-loca-20260522T031217/prototype/evidence/probe1-ligatures-result.json` exists; `jq .verdict` returns `"WORKS — addon-ligatures composes with the WebGL renderer"`
- The §Open Questions section of spec.md shows OQ-1/3/4/8/9 RESOLVED and OQ-2/5/7 assigned to design phase with leans
- The §4 non-goals section of spec.md is binding (the dev phase reads it back)
- `curl -s https://status.anthropic.com/api/v2/status.json | jq '.status.indicator'` returns `"none"` (operational) before retrying any subagent dispatch

If Anthropic API is still degraded when the new window opens: WAIT, do not thrash the retries — 6 consecutive 0-tool-use 529s already burned ~20 min of Cody's tokens. Surface status + ETA to Cody and let him call the timing.
