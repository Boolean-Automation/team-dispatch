// dispatch — FilterChip re-export (lives in Topbar but exported for direct use)
// The actual implementation is in Topbar.tsx. This file is a named slot
// per the plan's file list (web/src/issues/FilterChip.tsx).

// FilterChip is co-located in Topbar.tsx because it's tightly coupled there.
// This module re-exports nothing — the plan emits a file for it but the
// implementation belongs to Topbar. Keep this file as a documentation stub.

export {};
